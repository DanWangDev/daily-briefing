# All API keys and credentials are now configured via the Settings page
# and stored encrypted in the database.  No .env file is needed.
#
# To start the app:  python -m briefing
# Then open http://localhost:8000/settings to configure your keys.
