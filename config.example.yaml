# Only the database path is configured here.
# All other settings (LLM, schedule, email, API keys) are managed
# through the web UI at /settings and persisted in the database.
database:
  path: "./data/briefing.db"
